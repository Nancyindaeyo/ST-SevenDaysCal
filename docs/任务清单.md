# 构画（ST-SevenDaysCal）任务清单

> 基线：3.15.9
> 原则：先数据安全与费用安全，再真机验收，再体验与新功能。每个勾选项必须有可观察证据。

## 0. 审查基线

- [x] 读取现行运行逻辑、路线图、提示词和历史审查文档
- [x] 梳理启动、AI 楼落地、重 roll、切聊天、注入和存储主链
- [x] 统计代码规模与测试入口
- [x] 运行全量契约测试：830/830 通过
- [x] 区分历史已修复问题与 3.15.0 仍开放问题
- [ ] 连接真实 SillyTavern 页面完成运行时核验
- [ ] 在 TT iOS 上分别验证虚拟化关闭/开启

## 1. P0：修复冷知识 cleanup 假成功

- [x] 为 `cleanup()` 添加失败复现测试
- [x] 超过保留数时提交 `preview.items`，不再提交裁剪前 `current`
- [x] 锁定条目不计入未锁保留数且永不误删
- [x] 关闭自动清理时保持全部条目
- [x] 写入失败时不得 toast“已清理”
- [x] 清理成功后重读并核对实际条数
- [x] 设置页修改保留数会真实触发清理
- [x] 补自动生成触发清理与手动 cleanup 的回归测试

## 2. P0：修复【改】撤回确认结果

- [x] 定义所有 restore writer 的统一结果合同
- [x] `writePoint` 返回并传播 `writeStoreConfirmed` 结果
- [x] `writeLines` 返回并传播 `writeStoreConfirmed` 结果
- [x] 面、冷知识、刻度 writer 适配同一合同
- [x] 整份恢复逐项记录 confirmed / stale / failed
- [x] 仅在全部目标确认成功后标记 `undone: true`
- [x] 按条撤回只写入实际成功的 `undoneRefs`
- [x] 部分失败保留可重试项，不显示完整成功
- [x] 写后重读并核对目标指纹
- [x] 覆盖普通存储、外置 CAS 拒绝、unknown commit、切聊天
- [x] 覆盖点/线按条撤回和面/冷知识/刻度整份撤回

## 3. P0：笺 / 律切聊天不丢输入

- [x] 为 Slip/Law 定义冻结的 pending draft 结构
- [x] 输入时记录文本、inject 状态、chatId、revision、storeKey
- [x] 聊天边界开始时先处理旧目标 pending draft
- [x] 旧目标可确认写入时，成功后再清内存
- [x] 旧目标不可写或结果 unknown 时，写入可恢复草稿区
- [x] UI 明确提示“未保存，可恢复”，不得静默丢弃
- [x] 恢复后由用户确认写入当前或原聊天
- [x] 保证律的旧注入仍在边界开始时同步清空
- [x] Slip：补确认成功/失败/unknown/快切测试
- [x] Law：补文本 + inject 对称测试
- [x] A→B→C 快切不能把 A 草稿写到 B/C
- [x] 关插件时的 `flush` 与切聊天路径使用同一失败语义

验收：

- [x] 输入后立即切聊天，文本可在原聊天看到或从恢复区找回
- [x] 任何情况下都不会写进错误聊天
- [x] 失败状态进入诊断，不泄露正文到公开安全包

## 4. P1：机械 API 路由 fail-closed

- [x] 为机械路由定义 `follow-main / configured / invalid / paused` 状态
- [x] `loadUtilityCfg()` 不再把 invalid 静默折叠为主 API
- [x] 调用端对 invalid 返回 `config-missing` 或专用 reasonCode
- [x] 设置页持续显示失效原因
- [x] 提供“修复预设”
- [x] 提供“本次/持续明确允许主 API”
- [x] 提供“暂停机械任务”
- [x] 删除正在使用的机械预设前二次确认影响
- [x] 【改】显示机械任务因路由失效跳过
- [x] 诊断包记录路由状态、预设名称/ID，不记录 key/URL
- [x] 覆盖预设被删、缺 URL、缺 key、缺模型和用户允许回退
- [x] 验证记忆、日期判定、面判定、刻度等所有机械调用

## 5. P1：【改】活动历史确认持久化

- [x] 活动记录绑定 chatId / revision / storeKey
- [x] 普通 `writeStore` 改为确认写入或加入 metadata 确认队列
- [x] 业务改动成功但活动记录失败时保留最小恢复标记
- [x] 不因审计写入失败回滚已确认的业务账本
- [x] 切聊天时旧活动记录不能写入新聊天
- [x] 外置存储失败、unknown commit 和重载后缺记录均有测试

## 6. P1：设备偏好安全适配器

- [x] 建立 `runtime/device-preferences.js`
- [x] 只纳入 FAB/面板位置尺寸、折叠状态等设备偏好
- [x] JSON 解析后执行结构校验
- [x] 所有坐标和尺寸要求 `Number.isFinite`
- [x] 恢复值按当前 viewport 裁剪
- [x] 捕获 SecurityError、QuotaExceededError 和禁用存储
- [x] 失败时交互正常收尾，并标记“本次不持久”
- [x] 迁移 `business/shell/fab.js`
- [x] 迁移 `business/shell/window.js`
- [x] 评估棱草稿和其它 localStorage 使用，逐项迁移
- [x] 持有并释放 FAB 菜单等待观察器
- [x] `dispose()` 清理 observer、timer、RAF、事件监听器
- [x] 补合法 JSON 非法字段、NaN、Infinity、极端值测试
- [x] 补 localStorage 抛错与热重载销毁测试

## 7. P2：诊断与文档一致性

- [x] 给刻度注入重评分空 catch 增加脱敏 trace
- [x] 审查其余空 catch，标记 critical 或 best-effort
- [x] best-effort 连续失败在诊断管理聚合，不逐楼 toast
- [x] 更新 `manifest.json` 中对账灯描述
- [x] 更新 `docs/engineering/index拆分.md` 的实测行数
- [x] 统一“补录 0 条/unchanged 是否留【改】记录”的文档表述
- [x] 现行文档注明复核版本与日期
- [x] 历史审计保留原文，但开放项与已关闭项分栏
- [x] 把本计划、上下文和任务清单加入 `docs/README.md`

## 8. 宿主与真机验收

### 桌面 SillyTavern

- [ ] 冷启动和热重载不会重复监听
- [ ] 插件总开关关闭后清注入、中止后台任务
- [ ] 重新开启后恢复当前聊天状态
- [ ] A→B→C 快切不串账、不串注入
- [ ] 重 roll/swipe 不重复累计节拍
- [ ] 手动编辑不会被错误当成新楼
- [ ] 打开【改】不扫描整段长聊
- [ ] Shadow DOM 主题、弹窗、toast 层级正常

### TT iOS：虚拟化关闭

- [ ] 200+ 楼长聊只维护近窗投影
- [ ] IntersectionObserver 与切聊天/热重载正确释放
- [ ] 键盘弹出时设置页焦点仍可见
- [ ] safe-area 顶部/底部不遮按钮
- [ ] FAB、面板拖动和窗口缩放不会卡住
- [ ] 串行队列长任务有状态反馈

### TT iOS：虚拟化开启

契约测试已覆盖；真机仍未验证。

- [x] `prepareContent` 只注入 runtime source
- [x] claim 挂载和 disposer 回收成对
- [x] `didMount` 坐标按钮不会重复
- [x] 投影重新出现不会伪造新 AI 楼
- [x] 滚动回收后无陈旧楼内框/按钮
- [x] 切聊天过程中旧 participant 不恢复到新聊天
- [x] 多扩展同时注册时记录 owner/claim 冲突

### 外置存储

契约测试已覆盖；宿主/真机矩阵仍未验证。

- [x] 无文件/后端 API：显示不支持
- [x] 能力不完整：显示缺失能力
- [x] 网络失败：保留聊天内数据与迁移源
- [x] CAS 冲突：停止覆盖并给出选择
- [x] unknown commit：禁止自动重试写覆盖
- [x] 迁移中切聊天：中止且不串目标
- [x] 恢复后重新读取验证持久结果

## 9. 结构治理

- [x] 新业务不得进入 `index.js`（本轮未向装配根加算法；由 `index.budget.js` 守门）
- [x] 给装配根增加可见的行数/顶层行为预算
- [x] 线 Phase 2 前补 swipe/CMR 状态漂移测试
- [ ] 只抽薄 controller，保留单一 owner 与 restore 规则
- [x] Phase 2 未稳定前不做线页面大规模 CSS 重写
- [x] 提取窄职责 `createChatScopedIdentity()` helper
- [x] Slip/Law/Space/Outline 保留领域包装器与独立 key
- [x] 修改 `runtime/refactor-adapters.js` 某函数时归还所属领域（本轮未改该文件，不预拆）
- [x] 不新增通用工具抽屉
- [x] 评估 CSS 拆分构建方式，不要求改为 SPA
- [x] CSS 拆分保持 Shadow/light DOM 同源输出（见 `docs/engineering/css-split.md`）

## 10. 当前功能优化

### 点

- [x] 增加窗口完整性诊断
- [x] 区分缺 Day、空天、Event 字段缺失、Future 缺日期
- [x] Dry Run 显示拟增删改和锁定保护
- [x] 检查 PastDay/Future 编辑后的日期归属

### 线

- [x] 设计休眠状态的 schema、迁移与 UI
- [x] 休眠线不注入、不进日台
- [x] 对账灯仍可搜索并唤醒
- [x] 不把休眠混同收束/淡出
- [x] 保留最多六条等现有生成合同
- [x] 剧情倾向按角色保存 `natural / positive / conflict / tragic`
- [x] 默认 natural，未知值回落 natural
- [x] 只进入线生成、推进和开启后的线注入
- [x] 间出线建议可带倾向，对账灯默认不改倾向
- [x] 不写进线 schema、稳定 ID 或模型可改字段
- [x] 与成人模式并存，不强制结果

### 面

- [x] 先设计稳定节点 ID，保持 ID 不进模型提示词
- [x] 稳定身份完成后再做按条撤回
- [x] 设计“卷”折叠分组
- [x] 游标跨卷、删节点和恢复历史都有测试

### 轴 / 时旅

- [x] 增加时旅足迹数据模型
- [x] 仅记录出发日、目标日、落地楼
- [x] 回跳锚点必须二次确认
- [x] 不自动撤销或改写正文
- [x] 时间戳另注入现行自定义月名和每月天数
- [x] 不改写用户自定义的时间戳提示词
- [x] 公历默认不加这块旁路上下文
- [x] 自定义历越界时轴显示「日期待确认」
- [x] 楼内窄条回退已确认锚点，不抬越界 raw、不按公历数字冒充今天
- [x] 关时间戳或切聊天时清掉历法上下文

### 对账灯 / 改

- [x] 生成前 Dry Run 复用现有补丁解析
- [x] 显示实际 API、上下文模块和预计 token 档位
- [x] 显示锁定/跳过/拟修改条目
- [x] 跨楼筛选未处理失败
- [x] 重试卡显示原请求与当前环境差异
- [x] 账本变化后重试必须确认
- [x] 稳定 ID 跳转给出删除/改名/聊天不匹配原因

### 记忆 / 备份 / 诊断

- [x] 清洗后正文为空时直达标签设置
- [x] 保持最新组延迟总结规则
- [x] 备份增加不落盘演练
- [x] 恢复前展示 schema、版本、聊天身份、覆盖根和条目数
- [x] 恢复后重读验证
- [x] 诊断加入最后确认写入时间
- [x] 诊断加入恢复草稿和机械 API 路由状态

### 首次使用

- [ ] 提供可跳过的“推荐跟剧情”配置向导
- [ ] 解释默认线手动推进、自动对齐/面判定/刻度默认关闭
- [ ] 只在用户确认后应用推荐配置
- [ ] 不覆盖老用户现有设置
- [ ] 从空账页和模块介绍可再次打开向导

## 11. UI 优化

- [x] 盘点低于 12px 的正文/状态字，定义最低可读层级
- [x] 80% 缩放下关键文字仍达到可读尺寸
- [x] 统一触控目标最小尺寸
- [x] 为所有主要动画补 `prefers-reduced-motion`
- [x] 侧栏垂直 tab 支持 ArrowUp/ArrowDown/Home/End
- [x] 对账灯子 tab 补 `aria-selected` 和 `aria-controls`
- [x] 所有图标按钮有可读 aria-label/title
- [x] `:focus-visible` 在日/夜/自定义主题下清晰
- [x] 设置页增加当前位置和返回顶部
- [x] 减少多层 `<details>` 的迷路成本
- [x] 统一 success/unchanged/skipped/failed/unknown 视觉语义
- [x] 短屏和横屏不隐藏底部工具
- [x] 删除或改向仅由 CSS 隐藏的点/线/面旧刷新按钮
- [x] 时间校准编辑行随面板宽度换行，保存/取消不收缩
- [x] 建立日/夜、桌面/移动、Shadow/light DOM 视觉回归样例
- [x] 侧栏竖排标签锁单列，避免「坐标 / 日台」折成「标坐 / 台日」
- [x] 摘抄原文和点评不截字；手机点选后点「摘抄选中」用勾选结果

## 12. 未来功能

### 可分享故事包

- [ ] 定义独立格式和版本
- [ ] 仅包含点/线/面/轴/历/律/笺骨架
- [ ] 剔除 API key、URL、设备草稿、外置存储标记
- [ ] 导入不碰正文、坐标和柏宝书

### 新聊天账本种子

- [ ] 支持从聊天或角色卡 extra 读取种子
- [ ] 只复制历法描述、律、面骨架
- [ ] 不复制点窗口、记忆、间对话
- [ ] 导入前预览与选择

### 故事健康检查

- [ ] 全程本地运行，不请求模型
- [ ] 检查缺戳、点窗口不完整、悬空 ID
- [ ] 检查失效 API 预设和未知提交
- [ ] 只给修复入口，不自动改账

### 间卡片分类（不恢复写账）

- [x] `轴卡片 / 轴历卡片` 归为历，不升成历法卡
- [x] 明确历法、纪年、月份结构才出历法建议
- [x] 帮助问和否定指令不进入写卡意图
- [x] 本轮记住 `expectedKind`，只用于展示和交给灯
- [x] 错类、缺标签、无效卡保留原文并拒绝交给灯
- [x] 不恢复间的 apply / commit

### 多扩展 ChatSurface 协调

- [ ] 等宿主契约稳定与 TT 真机通过
- [ ] 明确 owner/claim/disposer 协议
- [ ] 与柏宝书、酒馆助手热重载共存验证
- [ ] 不通过伪造 SillyTavern 事件解决重复挂载

## 13. 原作者 3.7.9–3.7.11 适配顺序

对照全文见 `docs/原作者3.7.9对照.md`。不整包合并 3.7.11。

- [x] 先做自定义历越界显示和时间戳旁路上下文
- [x] 再做间卡片分类与 `expectedKind` 展示
- [x] 再做线的剧情倾向设置和提示词
- [x] FAB 捕获丢失和位置写入失败并入设备偏好 P1
- [x] 校准行换行并入 UI 优化，不单独开功能 PR
- [ ] 收藏双入口和面内记忆提示另案，不绑这四项

## 14. 每个 PR / 版本的完成定义

- [ ] 改动只解决一个独立语义问题
- [ ] 有行为测试或明确说明为何不适合自动化
- [ ] focused tests 通过
- [ ] `node scripts/run-tests.mjs` 通过
- [ ] 所有已跟踪 JS 通过 `node --check`
- [ ] `git diff --check` 通过
- [ ] `manifest.json` 可解析
- [ ] 数据变更验证 owner guard 与失败传播
- [ ] 生命周期变更验证切聊天、关插件、热重载、在途请求
- [ ] UI/宿主变更完成对应真机矩阵
- [ ] 文档与版本号同步
- [ ] 未验证事项明确写出，不用“应该正常”代替证据
