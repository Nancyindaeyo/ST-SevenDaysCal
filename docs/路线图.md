# 构画路线图

基线：**3.9.0**  
复核日期：**2026-09-16**

本文只记录尚未完成的方向，不重复罗列已经落地的增量。优先级综合考虑用户影响、串聊天/写坏数据风险、实现成本和现有测试基础；它不是版本承诺。

现行行为见《[运行逻辑与规则](./运行逻辑与规则.md)》，【改】与诊断现状见《[自动改账与诊断检查](./自动改账与诊断检查.md)》，工程拆分细节见《[index.js 拆分](./engineering/index拆分.md)》。

## 一、当前功能待优化方向

### P0：TT 虚拟化 ChatSurface 真机验收

3.8.1 已按宿主契约接上 `prepareContent` + `claim`、楼层按钮 `didMount` disposer；managed 模式不再用 MutationObserver / IntersectionObserver 猜节点归属，backfill 只重绘已经激活的投影。仍需要在真实 TT 打开虚拟化后走一遍挂载、滚动回收、切聊天和收藏钮。桌面 SillyTavern 的整串 DOM 不能代替这次验收。

#### 依据

- `runtime/chat-surface-participant.js`
- `business/inline/feature.js`
- `TauriTavern-main/docs/API/ChatSurface.md`

### P1：外置存储失败要明确

#### 方向

- 在纯 TT、无文件 API 或后端能力不完整时，设置页应明确显示“不支持/未就绪”，不能让用户误以为迁移已经成功。
- 切聊天加载、迁移、CAS 冲突、结果未知分别给出可行动的状态；未知结果不能静默重试覆盖。

#### 依据

- `runtime/external-chat-storage.js`
- `runtime/storage-panel.js`

### P2：切聊天期间隔离潜伏注入

写回侧已经有 chatId、revision、owner guard 和 abort，**旧结果写进新聊天不是当前缺口**。剩下的是加载窗口里旧线/面/刻度 extension prompt 还没撤掉：只有用户开着注入、切聊天后立刻打主楼，才可能把上一串账带进这次生成。默认注入关闭，窗口也短，所以排到 P2。

#### 目标

- 聊天边界开始时立即撤掉所有聊天相关潜伏注入。
- 新聊天数据加载并确认仍是当前聊天后，再按新聊天状态恢复注入。
- 时间戳这种不含聊天剧情的固定合同可单独评估是否需要保持，但实现上必须明确分类。

#### 验收

1. A 聊天开启线/面/刻度注入，切到 B 后在加载窗口立即发送，生成上下文不包含 A 的账。
2. B 加载完成后，注入只包含 B 的账。
3. 连续快速切换 A → B → C 时，B 的延迟刷新不能覆盖 C。

#### 依据

- `runtime/chat-changed.js`
- `business/lines/injection.js`
- `business/outline/injection.js`
- `business/ledger/inject.js`

### P2：面/冷知识按条撤回与 toast 遮挡

- 面节点没有稳定条目身份，冷知识虽有 `id` 但撤回仍按整份快照；没有稳定身份前不发明按条撤回。
- toast 迁入 shadow root 以真实主题冲突或遮挡反馈为触发条件；目前没有这类报告，保持现状。

## 二、代码待精简或拆分

原则：一次只拆一个有独立语义的块；不改变行为，不把 `index.js` 对半切，不新增只有一个实现的万能抽象。

### P1：宿主接线中块

按依赖风险分开提交：

1. `runtime/debug-payload.js`：迁出最近请求 payload 的复制和展示，风险最低。
2. `business/refresh/pace-host.js`：迁出 `readPaceSnapshot`、`paintPace`、interval 读取，DOM 作为端口注入。
3. `runtime/world-info-host.js`：承接角色/聊天/全局世界书解析、排除设置和面板端口，复用现有 `world-info-context.js` 与 `world-info-panel.js`。
4. 扩写 `runtime/generation-context.js`：迁入 participant identity、chat boundary epoch 与延迟回调闸。
5. `business/axis/date-detection-host.js`：最后迁日期检测的大 options 接线，避免先制造时旅/锚点循环依赖。

### P1：线模块 Phase 2

`business/lines/feature.js` 仍同时承担较多 generation、lifecycle、swipe/CMR 协调。下一阶段只抽薄 swipe/CMR controller：

- 保持单一 owner、chat revision 和 restore 规则。
- 一旦出现 swipe 状态漂移，停止继续拆分，先补契约测试。
- Phase 2 稳定前不做大规模 CSS/响应式重写。

### 每次拆分的门槛

1. 迁移前先确认目标块已有测试，缺失时补最小契约测试。
2. 每次只提交一块并运行 `node scripts/run-tests.mjs`。
3. 检查依赖方向：业务层不反向读取装配根，存储仍由 `store.js` / runtime 端口拥有。
4. 对生命周期改动额外验证切聊天与 abort；对数据改动额外验证 owner guard 和失败传播。

## 三、待新增功能

这些是候选能力，不代表已经排入版本。只有触发条件成立后才进入实现。工程项（1–2）和产品项分开列。

3.9.0 已落地 **笺**（侧栏私笺，永不注入）。一起装着[柏宝书](https://github.com/Nancyindaeyo/ST-MemoryBook)时，**记忆摘要、物品、NPC、场景地图、计划/悬念、故事时间、好感估计**都归对方。构画已经只读配料（眼下地点、进行中计划、在场人物），不要再做第二套。构画继续管：日程窗口、历法、平行线、大纲、番外、顾问、刻度到期、作者私笺。下面只列柏宝书没有、或必须两本对起来才有意义的东西。

### 候选 1：多扩展 ChatSurface 协调

当构画、柏宝书或酒馆助手热重载同时存在时，用明确 owner/claim 协议避免同一楼重复登记、重复 observer 和重复投影。只有宿主契约稳定且真机验收通过后实施。

### 候选 2：线模块 Phase 3 响应式样式

在 Phase 2 controller 稳定后，为线页面和楼内线块补语义化 class、移动端布局和可测的状态样式。它不能与 lifecycle 重构混在同一版本。

### 候选 3：对账灯

不是新账本。构画自己的点 / 刻度 / 线 / 轴会打架；装着柏宝书时，再加只读交叉：点的地点 vs 柏宝书 `state.location`，刻度伤情 vs `protagonist.condition`，线的「近日」vs 未核销计划。只提示，不改任何一侧。

- **触发**：同一天里点写「在家养伤」、刻度已到期、线还写「今夜赴约」；或点还在旧宅、柏宝书地点已经换城。
- **不是**：自动改账、每楼重查、也不把柏宝书条目抄进构画。点一下跳到构画那一条；柏宝书侧只给对照原文。

### 候选 4：日台（今日演出单）

把「今天该演什么」收成一屏：点的今天、将至节日、到期刻度、时机写近日的线。柏宝书台账是「世界现在怎样」（已发生状态）；日台是「作者打算这几天演什么」。

- **触发**：写下一楼前要同时翻点页、轴、刻度、线。
- **不是**：第二份摘要，也不注入主楼。纯作者驾驶舱；冲突条目可链到对账灯。

### 候选 5：卷（面的篇章）

面是一串节点。长篇会变成几十个 Beat 一条绳子，作者想按「上卷 / 这一季」折叠。柏宝书的摘要森林是**已经发生的历史目录**；卷是**还没演完的大纲分组**。

- **触发**：面已经长到翻不动，你开始用节点标题当章名。
- **不是**：第二套记忆森林，也不自动按楼层切章。只给面加可折叠分组和游标落在哪一卷；生成仍写节点，不把卷名塞进稳定 ID。

### 候选 6：律（短合同）

几条必须遵守的作者红线或文风句（「不要写怀孕」「称呼保持您」）。柏宝书的排除名单管的是记忆清洗，不管「故事里不许发生什么」。

- **触发**：同一条禁忌你反复在间或角色备注里重说，主楼还是会踩。
- **不是**：完整人设、也不是每楼自动审稿。默认不注入；打开才当 SYSTEM 短合同。和笺的区别：笺永不进模型，律是你明确勾选才进。

## 当前明确不做

- 不每楼自动重查点/线/面；继续使用节拍和手动入口。
- 不让本轮拍或间引导自动代发主楼。
- 不把棱合并为其它多媒体小剧场，也不自动绑定角色卡。
- 不把稳定 ID 放进模型提示词，不为此单独升级生成 schema。
- 不做小时级刻度或子日对齐；当前时辰只用于显示和上下文。
- 不为了压 token 再截断已经按规则保留的正文/记忆。
- 不把装配根拆分当新功能前置条件；重构与产品交付分别排期。
- 不重复柏宝书已有的物品、NPC、场景地图、计划/悬念、摘要时间线；需要时只读对照，不另建一本。

## 建议顺序

1. 在打开虚拟化的 TT 真机上验收 ChatSurface 挂载/回收。
2. 一次一个宿主中块：先 `debug-payload`，再节拍、世界书、generation-context、日期检测。
3. 外置存储失败态按出现顺序补齐；切聊天潜伏注入等真的碰到加载窗口误注入再做。
4. 多扩展 ChatSurface 协调和线 Phase 3 只在对应触发条件成立后启动。
5. 对账灯 / 日台 / 律只在你真缺那块时启动，不跟拆分抢版本；下一步按从易到难：律 → 日台 → 对账灯。
